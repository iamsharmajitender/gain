# Remove draft (publish insight)

Read and follow **`.cursor/skills/remove-draft/SKILL.md`** completely.

**Phase 1 only** unless the user already gave **row numbers** to apply (e.g. `1`, `1 and 2`, `all rows`, or `none`).

1. Identify the target insight (from `@` attached files, or list all `draft: true` in `docs/insights/` and ask).
2. Scan all of `docs/` for inbound link opportunities to `/insights/<slug>`.
3. Return the report: **Insight** table + single numbered **Suggested inbound links** table (High → Medium → Optional, all in one table). **Do not edit files yet.**

If the user attached a specific insight file, use that as the publish target.

End by asking the user to **reply with row numbers** (`1`, `all rows`, `none`), or **`abort` / `quit` / `exit` / `cancel`** to stop with no changes anywhere.
