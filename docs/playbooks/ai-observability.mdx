---
slug: ai-observability
title: AI Observability
description: Five signals, five storage tiers, five retention policies — and four consumers that read them.
date: 2026-01-15
authors: [iamsharmajitender]
tags:
  - platforms-engineering
  - observability
---

import Details from '@theme/Details';

<div className="gain-doc-header">
  <h1 className="gain-doc-title">AI Observability</h1>
  <div className="gain-doc-subtitle">
    Five signals, five storage tiers, five retention policies: and four consumers that read them.
  </div>
</div>

## Design for AI Observability

<div className="gain-section-lead">
  AI observability is not a dashboard. It is a capture-and-retention architecture. Each signal has a
  different consumer, retention window, and blast radius if you get it wrong. Read left to right:{' '}
  <strong>capture → store → consume</strong>.
</div>

<div className="gain-diagram-wrap">

```mermaid
flowchart LR
    q["Query"] --> app["Agentic app / RAG service / OTel SDK"]

    subgraph capture["Capture (in the request path)"]
        app --> |logs| col["OTel Collector<br/>route · sample · redact"]
        app --> |metrics| col
        app --> |traces| col
        app --> |"raw prompt/response"| col
        app --> |"audit record"| col
    end

    subgraph storage["Storage tiers"]
        logstore[("Log Store")]
        tsdb[("TSDB")]
        tracestore[("Trace Store")]
        restricted[("Restricted Store")]
        audit[("Audit Log")]
    end

    col --> |"structured logs · 30 d"| logstore
    col --> |"metrics · 13 m"| tsdb
    col --> |"sampled traces · 30 d"| tracestore
    col --> |"encrypted · 90 d"| restricted
    col --> |"immutable · 7 y"| audit

    subgraph consume["Consumers"]
        dash[("Dashboards")]
        slo[("SLO + burn-rate alerts")]
        drift[("Drift detector")]
        replay[("Regulator replay")]
    end

    logstore --> dash
    tsdb --> dash
    tsdb --> slo
    tracestore --> dash
    tracestore --> drift
    restricted --> drift
    audit --> replay

    classDef query fill:#dbeafe,stroke:#93c5fd,color:#1e293b
    classDef entry fill:#ede9fe,stroke:#c4b5fd,color:#1e293b
    classDef collector fill:#ffedd5,stroke:#fdba74,color:#1e293b
    classDef logs fill:#dbeafe,stroke:#93c5fd,color:#1e293b
    classDef metrics fill:#dcfce7,stroke:#86efac,color:#1e293b
    classDef traces fill:#ede9fe,stroke:#c4b5fd,color:#1e293b
    classDef raw fill:#fee2e2,stroke:#fca5a5,color:#1e293b
    classDef audit fill:#f1f5f9,stroke:#94a3b8,color:#1e293b
    classDef dash fill:#dbeafe,stroke:#93c5fd,color:#1e293b
    classDef slo fill:#fef3c7,stroke:#fcd34d,color:#1e293b
    classDef drift fill:#ede9fe,stroke:#c4b5fd,color:#1e293b
    classDef replay fill:#f1f5f9,stroke:#94a3b8,color:#1e293b

    class q query
    class app entry
    class col collector
    class logstore logs
    class tsdb metrics
    class tracestore traces
    class restricted raw
    class audit audit
    class dash dash
    class slo slo
    class drift drift
    class replay replay

    style capture fill:#eff6ff,stroke:#93c5fd,color:#1e293b
    style storage fill:#f0fdf4,stroke:#86efac,color:#1e293b
    style consume fill:#fefce8,stroke:#fcd34d,color:#1e293b
```

</div>

<br/>

| Signal | Store | Retention | Consumer |
| --- | --- | --- | --- |
| **Structured logs** | Log store | 30 d | Dashboards |
| **Metrics** | TSDB | 13 mo | Dashboards, SLO alerts |
| **Sampled traces** | Trace store | 30 d | Dashboards, drift detector |
| **Raw prompt/response** | Restricted store | encrypted, 90 d | Drift detector |
| **Audit record** | Audit log | immutable, 7 y | Regulator replay |

## Key practices

<Details className="gain-accordion" summary="Capture in the request path">
  Instrument at the point of inference through an OTel SDK into a collector on the hot path, not as a
  batch job afterward. Routing, sampling, and redaction happen once in the collector.
</Details>

<Details className="gain-accordion" summary="Five tiers, five retention policies">
  Do not collapse logs, metrics, traces, raw prompts, and audit records into one bucket. Retention is
  a governance decision: raw prompts are short and encrypted; audit logs are long and immutable.
</Details>

<Details className="gain-accordion" summary="Four consumers, four questions">
  Dashboards answer <em>what is happening now</em>. SLO alerts answer <em>are we burning budget</em>.
  Drift detection answers <em>is quality eroding</em>. Regulator replay answers <em>can we prove what
  we did</em>.
</Details>

<Details className="gain-accordion" summary="Redact at the collector">
  Strip PII and sensitive payloads before persistence. The collector is the privacy boundary: not the
  dashboard.
</Details>

<Details className="gain-accordion" summary="Sample expensive signals">
  Metrics are always-on; traces and raw prompt/response are sampled or gated. Instrumentation is not
  free on the request path.
</Details>

For the framework view, see [G.A.I.N Observability](/frameworks/gain-observability). For the full
architecture breakdown, see
[Insights → AI Observability in Enterprise](/insights/ai-observability-in-enterprise).
